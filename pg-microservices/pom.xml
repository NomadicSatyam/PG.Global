<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">

	<modelVersion>4.0.0</modelVersion>
	<groupId>com.satyampay.pg</groupId>
	<artifactId>pg-microservices</artifactId>
	<version>0.0.1-SNAPSHOT</version>
	<name>pg-microservices</name>
	<description>A project that enables seamless payment transfers between different banks.</description>
	<packaging>pom</packaging>

	<!-- PROPERTIES -->
	<properties>
		<java.version>17</java.version>
		<maven-compiler.version>3.12.1</maven-compiler.version>
		<project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
		<spring-boot-maven-plugin.version>3.2.5</spring-boot-maven-plugin.version>
		<maven-failsafe-plugin.version>3.5.0</maven-failsafe-plugin.version>
		<build-helper-maven-plugin.version>3.6.0</build-helper-maven-plugin.version>
		<maven.maven-compiler.source>17</maven.maven-compiler.source>
		<maven.maven-compiler.target>17</maven.maven-compiler.target>
	</properties>

	<!-- MODULES -->
	<modules>
		<module>pg-common-libraries</module>
		<module>pg-microservice-merchant</module>
		<module>pg-core</module>
		<module>pg-microservice-payment-orchestrator</module>
		<module>pg-microservice-cardpayment</module>
		<module>pg-microservice-upipayment</module>
		<module>pg-microservice-netbanking</module>
		<module>pg-microservice-transaction</module>
		<module>pg-microservice-settlement</module>
		<module>pg-microservice-refund</module>
		<module>pg-microservice-fraud</module>
	</modules>

	<dependencyManagement>
		<dependencies>
			<dependency>
				<groupId>com.satyampay.pg</groupId>
				<artifactId>pg-common-libraries</artifactId>
				<version>${project.version}</version>
				<type>pom</type>
				<scope>import</scope>
			</dependency>
		</dependencies>
	</dependencyManagement>


	<profiles>
		<profile>

			<!-- PROFILE - TEST INTEGRATION -->
			<!--
                This profile is used to execute integration tests.
                Integration tests should be located in the folder src/test-integration
             -->
			<id>test-integration</id>

			<build>
				<plugins>
					<!-- MAVEN FAILSAFE PLUGIN -->
					<plugin>
						<groupId>org.apache.maven.plugins</groupId>
						<artifactId>maven-failsafe-plugin</artifactId>
						<version>${maven-failsafe-plugin.version}</version>
						<executions>
							<execution>
								<id>integration-test</id>
								<goals>
									<goal>integration-test</goal>
									<goal>verify</goal>
								</goals>
								<configuration>
									<testSourceDirectory>src/test-integration</testSourceDirectory>
									<includes>
										<include>**/*.java</include>
									</includes>
									<failIfNoTests>false</failIfNoTests>
								</configuration>
							</execution>
						</executions>
					</plugin>

					<!-- BUILD HELPER PLUGIN -->
					<plugin>
						<groupId>org.codehaus.mojo</groupId>
						<artifactId>build-helper-maven-plugin</artifactId>
						<version>${build-helper-maven-plugin.version}</version>
						<executions>
							<execution>
								<id>add-test-source</id>
								<phase>generate-test-sources</phase>
								<goals>
									<goal>add-test-source</goal>
								</goals>
								<configuration>
									<sources>
										<source>src/test-integration</source>
									</sources>
								</configuration>
							</execution>
						</executions>
					</plugin>
				</plugins>
			</build>
		</profile>
	</profiles>


	<!-- BUILD -->
	<build>

		<!-- PLUGINS -->
		<plugins>
			<plugin>
				<groupId>org.codehaus.mojo</groupId>
				<artifactId>properties-maven-plugin</artifactId>
				<version>1.0.0</version>
				<executions>
					<execution>
						<phase>initialize</phase>
						<goals>
							<goal>write-project-properties</goal>
						</goals>
						<configuration>
							<outputFile>${project.build.outputDirectory}/environment.properties</outputFile>
						</configuration>
					</execution>
				</executions>
			</plugin>
		</plugins>

		<!-- PLUGIN MANAGEMENT -->
		<pluginManagement>
			<plugins>

				<!-- COMPILER PLUGIN -->
				<plugin>
					<groupId>org.apache.maven.plugins</groupId>
					<artifactId>maven-compiler-plugin</artifactId>
					<version>${maven-compiler.version}</version>
					<configuration>
						<source>${java.version}</source>
						<target>${java.version}</target>
						<compilerArgs>
							<arg>-parameters</arg>
						</compilerArgs>
					</configuration>
				</plugin>


				<plugin>
					<groupId>org.springframework.boot</groupId>
					<artifactId>spring-boot-maven-plugin</artifactId>
					<version>${spring-boot-maven-plugin.version}</version>
				</plugin>

			</plugins>

		</pluginManagement>
	</build>

</project>
